// 문자열 s에 나타나는 문자를 큰 것부터 작은 순으로 정렬해 새로운 문자열을 리턴하는 함수 solution을 완성하세요.
// s는 영문 대소문자로 구성되어있으며, 대문자는 소문자보다 작은 것으로 간주된다.

//s - Zbcdefg
//return - gfedcbZ

function solution(s) {
  return s.split("").sort().reverse().join("");
}

//1. split() 메서드를 사용하여 문자열을 문자 배열로 변환합니다.
"Zbcdefg" = ["Z", "b", "c", "d", "e", "f", "g"]

//2. sort() 메서드를 사용하여 배열을 정렬합니다. 기본적으로 문자열은 유니코드 순서로 정렬되므로 대문자가 소문자보다 먼저 오게 됩니다.
// 기본적으로 sort()는 유니코드 코드 포인트(UTF-16 코드 값)에 따라 정렬합니다.

["Z", "b", "c", "d", "e", "f", "g"].sort()
// 결과: ["Z", "b", "c", "d", "e", "f", "g"] → 오름차순 정렬

//3. reverse() 메서드를 사용하여 배열을 역순으로 정렬합니다.

//4. sort()로 오름차순 정렬된 배열을 뒤집으면 내림차순 정렬이 된다.
//즉, 문자 코드 기준으로 큰 문자부터 작은 문자 순이 된다.

//5. join() 메서드를 사용하여 배열을 다시 문자열로 결합한다.
